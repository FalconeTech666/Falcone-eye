# Falcone-eye
Falcone eye is a background Windows service that automatically monitors new files appearing in user directories and creates secure backups of them. The script is designed for situations where it is important to preserve any files that appear on the PC, even if they are later deleted by the user.
